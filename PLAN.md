# DETOUR 5주 실행 계획

이 문서는 DETOUR를 5주 동안 학습하면서 실제 서비스로 완성하기 위한 기준 계획입니다.

## 사용 방법

- 주차가 시작되면 해당 주의 Session 체크리스트를 기준으로 진행합니다.
- 완료한 항목은 `- [x]`로 표시합니다.
- 주차 회고가 끝나면 완료한 체크 항목은 삭제하고, 주간 회고 문서에 결과를 보존합니다.
- 사용자가 "시작하자"라고 하면 그날의 작업 문서와 구체적인 체크리스트를 만듭니다.
- 사용자가 "일주일이 끝났어"라고 하면 공부·구현·GitHub 기록을 점검합니다.

## 작업 리듬

주 3~4회, 회당 3시간을 기준으로 한다. 한 주는 Session 3개로 나눈다.

각 세션은 아래 구조로 진행한다.

```text
0:00 – 2:15   구현 (AI가 주도, 옆에서 보며 질문)
2:15 – 2:45   ★ 한 군데는 직접 — 일부러 비워둔 곳을 채운다
2:45 – 3:00   study/에 "오늘 배운 것 3줄" 기록
```

★ 부분이 학습의 핵심이다. 전체를 직접 쓰면 5주 안에 안 끝나고, 전부 AI가 쓰면 남는 게 없다.
매주 한 곳은 반드시 직접 채운다.

## 서비스 범위

**목표는 대한민국 전국, 시작은 서울 성수동 한 곳.**

Week 3에서는 성수동 고정 데이터로 추천 로직을 맞춘다. 처음부터 전국으로 하면
추천이 이상할 때 데이터 문제인지 로직 문제인지 구분할 수 없다.
로직이 맞는 것을 확인한 뒤 Week 4에서 좌표 반경 검색으로 바꾸면 전국이 된다.

데이터 소스별 커버 범위가 다르다는 점이 이 판단의 근거다.

| 데이터 | 커버 범위 | 전국 서비스 가능 |
| --- | --- | --- |
| 카카오맵 (장소·좌표·경로) | 전국 | 가능 |
| 기상청 (날씨) | 전국 | 가능 |
| 서울시 실시간 도시데이터 (혼잡도) | 서울 주요 120곳 | 서울 한정 |

혼잡도는 확장 기능이므로 없어도 서비스가 성립한다. 핵심 MVP는 전국에서 동작한다.

## Week 1 — Frontend와 첫 화면

목표: 빌드 도구 없이 `index.html` 한 파일에서 사용자가 출발지·도착지·남은 시간·예산·활동을 입력하면 예시 추천 결과 카드가 뜬다.

- [ ] Session 3 남은 것: Week 1 공부·구현 점검, 회고, GitHub 기록

완료 기준: 입력 후 버튼을 누르면 조건과 추천 이유가 일치하는 예시 카드 2개가 보이며, 모바일에서도 입력·결과를 읽기 쉽다.

## Week 2 — 배포와 서버의 시작

목표: 진짜 URL을 확보하고, FastAPI가 주소를 좌표로 바꿔 돌려준다.

**배포를 이번 주로 당긴 이유**: `index.html`은 정적 파일 한 개라 GitHub Pages에 올리는 데 5분이면 된다.
지금 배포하면 남은 4주 내내 "현재 상태"를 링크로 볼 수 있고, HTTPS·CORS 문제를 마지막 주가 아니라 지금 만난다.
그리고 브라우저 위치 기능(geolocation)은 `file://`에서 아예 작동하지 않아, 배포해야 테스트가 가능하다.

### Session 1 — 배포 먼저

- [ ] GitHub Pages로 `index.html` 배포, 실제 URL 확보
- [ ] 휴대폰으로 그 URL을 열어 입력·결과·모바일 레이아웃 확인
- [ ] README에 배포 URL 추가
- [ ] 공부: HTTP, Request, Response, JSON, HTTPS가 무엇인지

### Session 2 — 열쇠와 데이터 준비

- [ ] 카카오 개발자 등록, 앱 생성, REST API key 발급
- [ ] 기상청 API허브 인증키 발급 (Week 5 확장 기능용, 미리 받아둔다)
- [ ] 장소 유형별 체류 시간·비용 규칙표 확정 (아래 "장소 데이터 규칙" 참고)
- [ ] 성수동 후보 장소를 카카오 카테고리 검색으로 수집하고, 추천 후보 20개를 사람이 선별

### Session 3 — FastAPI 첫 걸음

- [ ] Python 가상환경과 FastAPI 설치, `/health` Endpoint 구현
- [ ] Pydantic Request/Response Model 작성
- [ ] `POST /geocode` 구현 — 주소·장소명을 카카오맵 API로 좌표 변환
- [ ] Swagger `/docs`에서 직접 호출해보기
- [ ] `.env`에 key 보관, `.gitignore` 확인 — key는 절대 커밋하지 않는다

완료 기준: 배포된 URL이 존재하고, 로컬 FastAPI가 주소를 좌표 JSON으로 돌려주며, API key는 브라우저에 노출되지 않는다.

## Week 3 — 서버 연결과 장소 데이터

목표: 브라우저가 내 서버에 요청을 보내고, 서버가 성수동 장소 데이터로 추천을 돌려준다.

### Session 1 — 장소 데이터 파이프라인

- [ ] 수집한 원본 데이터를 정제 (결측값·중복·잘못된 값 정리)
- [ ] Feature 생성: `placeType` → 규칙표에서 `stayMinutes`, `cost` 도출
- [ ] 활동 분류(`산책`/`카페`/`구경`/`문화`)와 `tags` 부여
- [ ] 공부: Feature Engineering이 무엇인지 — 원본 데이터에서 추천에 쓸 값을 만들어내는 일

### Session 2 — /recommend와 연결

- [ ] `POST /recommend` 구현 — `docs/WEEK2_API_CONTRACT.md`의 형식 그대로
- [ ] `index.html`의 `places` 상수를 지우고 `fetch()`로 교체
- [ ] CORS 설정과 디버깅

### Session 3 — 서버 배포

- [ ] FastAPI 서버 배포 (Render 등), 환경변수로 API key 관리
- [ ] 배포된 Frontend ↔ 배포된 Backend 연결 확인
- [ ] Figma 화면 06(주소 못 찾음), 07(서버 오류) 구현
- [ ] Dataset 출처·갱신 시각·지역 한계를 기록

완료 기준: 배포된 웹에서 입력하면 배포된 서버를 거쳐 추천이 돌아온다. 주소 못 찾음과 서버 오류가 각각 다른 화면으로 처리된다.

## Week 4 — 추천 점수와 전국 확장

목표: 실제 경로 시간과 설명 가능한 점수로 TOP 3를 계산하고, 성수동 고정에서 벗어난다.

### Session 1 — 실제 경로 시간

- [ ] 카카오 경로 API로 현재 위치 → 후보 → 목적지 시간 조회
- [ ] 우회에 드는 추가 시간·거리 계산 (`travelMinutes`를 진짜 값으로 교체)
- [ ] 공부: Feature, Normalization, Weight, Score, Ranking

### Session 2 — 점수 계산

- [ ] 시간·예산·거리 적합도를 0~1로 정규화 ★ 이 함수는 직접 채운다
- [ ] 사용자 활동 Weight와 최종 Score 계산
- [ ] Ranking과 TOP 3 응답, 점수 근거 기반 추천 이유 자동 생성
- [ ] 카드를 2개에서 3개로

### Session 3 — 전국으로

- [ ] 고정 20개 대신 출발지 좌표 반경의 카카오 카테고리 검색으로 후보 수집
- [ ] 성수동 외 지역(예: 부산, 대전)에서 동작 확인
- [ ] 조건을 바꿔가며 순위와 추천 이유가 합리적으로 바뀌는지 테스트

완료 기준: 입력 조건을 바꾸면 실제 이동 시간·예산·활동에 따라 순위와 추천 이유가 바뀌고, 성수동이 아닌 지역에서도 결과가 나온다.

## Week 5 — 지도, 확장 기능, 사용자 테스트

목표: 지도를 붙이고 확장 기능 하나를 넣은 뒤, 실제 사용자에게 보여준다.

### Session 1 — 지도

- [ ] 카카오맵 JavaScript SDK로 후보 장소와 경로 표시 (Figma 화면 09)
- [ ] 브라우저 현재 위치(geolocation) 연결, 권한 거부 화면 구현 (Figma 화면 08)
- [ ] 모바일 레이아웃과 Touch Target 재점검

### Session 2 — 확장 기능 하나

아래 중 **하나만** 고른다. 핵심 MVP가 이미 동작하므로 `docs/MVP_DECISION_RULES.md`의 조건을 만족한다.

- [ ] (선택 A) 날씨 — 기상청 단기예보로 강수·하늘 상태를 받아 실내/실외 가점
- [ ] (선택 B) 혼잡도 — 서울시 실시간 도시데이터. 서울 한정이며 데이터 없는 장소는 "혼잡도 정보 없음" 표시
- [ ] 선택하지 않은 쪽은 확장 아이디어로 남긴다

### Session 3 — 사용자 테스트와 마무리

- [ ] 3~5명 사용자 테스트 — 목적지 설정, 추천 이유 이해 여부 확인
- [ ] 발견한 문제 1~2개 수정
- [ ] API 호출량·오류 로그 점검
- [ ] README·DEVLOG·포트폴리오 정리와 최종 회고

완료 기준: 배포된 웹에서 입력 → 실제 경로 기반 TOP 3 → 지도 확인이 가능하고, 주요 오류 상태를 설명할 수 있다.

## 장소 데이터 규칙

체류 시간과 비용은 조사하지 않고 유형별 규칙으로 정한다. 규칙표와 그 근거는
[`docs/PLACE_RULES.md`](docs/PLACE_RULES.md)에 있다.

핵심만 옮기면 이렇다.

- 카카오는 이름·주소·좌표·카테고리를 주지만 **체류 시간과 비용은 주지 않는다.** 세상에 없는 데이터다.
- 장소마다 조사해도 정확해지지 않는다. 규칙으로 정하면 값이 일관되고 근거를 설명할 수 있다.
- **이동 시간은 규칙표에 없다.** 출발지마다 다르므로 Week 4에서 카카오 경로 API가 계산한다.
- 사람이 할 일은 후보 걸러내기, 유형 분류, 규칙표 조정 세 가지뿐이다. 30~60분.

## MVP 데이터 연동 원칙

| 데이터 | 사용 목적 | 적용 시점 | 주의할 점 |
| --- | --- | --- | --- |
| 카카오맵 API | 주소·장소명 → 좌표, 주변 후보 검색, 실제 경로 시간 | Week 2~4 | REST key는 FastAPI 서버의 `.env`에만 보관 |
| 기상청 API | 강수·하늘 상태로 실내/실외 가점 | Week 5 (선택) | 전국 커버. 좌표를 격자로 변환해야 함 |
| 서울시 실시간 도시데이터 | 지역 단위 혼잡도 | Week 5 (선택) | 서울 120곳 한정. 지역 인구 혼잡도이지 특정 카페의 빈자리가 아님 |

키는 모두 브라우저가 아닌 서버의 `.env`에 보관한다. `.env`는 커밋하지 않는다.

## 확장 아이디어 — MVP 완료 후 검토

- 혼잡도 선호 선택: `조용한 곳`(낮음 가점) / `적당히 활기찬 곳`(보통 가점) / `요즘 인기 있는 곳`(화제성 가점).
  혼잡도는 품질의 절대 기준이 아니라 사용자 선호와 맞는지 판단하는 Feature다.
  데이터가 없는 장소는 점수를 과하게 올리거나 내리지 않고 `혼잡도 정보 없음`을 표시한다.
- Grok(xAI): 이미 시간·거리 조건을 통과한 후보에 화제성을 더하는 **보조** 역할.
  후보를 직접 결정하는 엔진으로 쓰지 않는다. 매 요청마다 검색하면 비용과 결과 변동이 크므로
  하루 단위로 캐시하고 출처·갱신 시각을 남긴다.
- Instagram: MVP에서 연동하지 않는다. 공식 API는 Professional 계정 중심이라 일반 계정에 접근할 수 없고,
  지역 인기 게시물을 안정적으로 순위화하기 어렵다. 스크래핑은 약관 위반이므로 하지 않는다.
- LLM으로 추천 이유 문장 다듬기
- 사용자 행동 기반 개인화

## 핵심 추천 파이프라인

```text
장소·경로 데이터
→ Feature Engineering
→ Normalization
→ 사용자 Weight
→ Score
→ Ranking
→ 추천 이유
```
