# 2026-08-29 — Week 1 Day 3~6

- 주제: 장소 데이터 구조 정리와 결과 카드 정직하게 만들기
- 오늘의 목표: `index.html`의 장소 데이터 필드를 Week 3·4까지 이어질 이름으로 정하고, 카드가 보여주는 숫자를 입력 조건과 일치시킨다.

## 오늘의 체크리스트

- [x] 장소 20개의 필드 구조 확정 (`id` `area` `stayMinutes` `travelMinutes` `tags` 추가)
- [x] 필터를 총 소요 시간(체류 + 이동) 기준으로 정리
- [x] 하드코딩 Match 퍼센트 삭제
- [x] Loading·Empty·Error 세 가지 상태 화면 구분
- [x] 모바일 375px 점검
- [x] Week 2 API 요청·응답 형식 문서화
- [x] 학습 기록의 React 예제를 실제 vanilla JS 코드로 교체

## 공부한 것

- **필드 이름은 값의 뜻과 맞아야 한다.** `distance`라는 이름에 "분" 단위 값이 들어 있었고, 카드는 `78분`과 `예시 이동 시간 13분`을 나란히 보여줬다. 사용자는 이걸 78 + 13 = 91분으로 읽는다. 필터 계산 자체는 틀리지 않았지만, 이름이 애매하면 화면도 애매해진다.
- **State는 React에만 있는 개념이 아니다.** `index.html`에서는 입력칸 자체(`.value`)가 값을 기억한다. 대신 화면을 다시 그리는 일은 내가 `innerHTML`로 직접 해야 한다.
- **정렬 기준이 곧 제품의 주장이다.** 총 소요 시간이 짧은 순으로 정렬하면, 90분이 비어 있어도 20분짜리 코스를 먼저 추천한다. "남는 시간을 나만의 우회로로"라는 제품 전제와 어긋난다.
- **한국어 줄바꿈**은 `word-break: keep-all`이 필요하다. 없으면 모바일에서 "우회로 / 로."처럼 단어 중간이 잘린다.

## 구현한 것

- `places` 20개를 새 구조로 다시 작성했다. `minutes` → `stayMinutes` + `travelMinutes`로 분리하고, `id` `area` `tags`를 추가했다. 총 소요 시간은 이전과 같게 유지했다.
- 카드가 `총 48분 · 7,000원` / `이동 7분 + 머무는 시간 41분 · 남은 60분 중 12분 여유`를 보여준다.
- `92 - index * 7`로 계산되던 가짜 Match 퍼센트를 삭제했다. 입력 조건과 아무 상관 없는 숫자를 "% Match"로 보여주는 것은 "설명 가능한 추천"이라는 이 제품의 정체성과 맞지 않는다.
- 정렬을 총 소요 시간 내림차순(= 남은 시간을 알차게 쓰는 순)으로 바꿨다.
- Loading(찾는 중), Empty(조건에 맞는 코스 없음), Error(입력을 고쳐야 함) 세 상태를 색과 문구로 구분했다. Loading은 Week 2에 `fetch()`가 들어갈 자리를 미리 만들어 둔 것이다.
- 도착지 조사 처리를 추가했다. `성수역로` → `성수역으로`, `서울숲로` → `서울숲으로`.
- `docs/WEEK2_API_CONTRACT.md`를 새로 썼다.

## 고민과 선택

- **오늘 API를 넣을까?** 넣지 않기로 했다. 이유는 세 가지다. (1) REST key를 `index.html`에 적으면 페이지 소스 보기만으로 노출된다. (2) 카카오 로컬 REST API는 브라우저에서 직접 부르면 CORS로 막히는 경우가 많아 결국 서버가 필요하고, 그게 Week 2다. (3) API를 붙여도 그 위의 추천 로직이 틀려 있으면 데이터만 진짜가 된다. 로직을 먼저 맞추고 데이터를 갈아끼우는 순서가 안전하다.
  - 대신 `docs/WEEK2_API_CONTRACT.md`에 요청·응답 형식을 확정해서, 다음 주에 FastAPI만 얹으면 되게 했다. 응답 필드 이름을 `index.html`의 `places`와 똑같이 맞췄기 때문에 카드 렌더링 코드는 바꾸지 않아도 된다.
- **정렬을 "시간을 알차게 쓰는 순"으로 바꾼 것의 위험**: 가장 잘 맞는 코스가 여유를 12분만 남긴다. 실제 약속이라면 빠듯하게 느낄 수 있다. 지금은 여유 분을 카드에 그대로 표시해서 사용자가 판단하게 두었다. Week 4에서 점수를 만들 때 "버퍼가 너무 적으면 감점"을 가중치로 넣을지 검토한다.
- **`index.html`과 `frontend/`가 달라지는 문제**: 의도한 것이다. `frontend/`는 Day 2에 만든 Next.js 실험본이고 이번 주 제출 범위가 아니다. 두 파일을 지금 동기화하면 규칙이 다른 코드를 이중 관리하게 된다. 어느 쪽을 본선으로 삼을지는 Week 2를 시작할 때 한 번에 결정한다.
- **새 아이디어 분류** (`docs/MVP_DECISION_RULES.md` 기준): `tags` = 핵심(추천 이유의 재료), Loading 상태 = 핵심(Day 6 항목), `indoor`·`crowd` 필드 = 확장(넣지 않음), 정규화 기반 점수 = 보류(Week 4).

## 막힌 점과 해결 과정

- 없음.

## 테스트 결과

브라우저에서 `index.html`을 직접 열어 확인했다.

| 시나리오 | 결과 |
| --- | --- |
| 서울숲역 → 성수역 / 60분 / 10,000원 / 카페 | 로스터리 카페(총 48분, 여유 12분), 베이커리 카페(총 36분, 여유 24분) |
| 90분 / 무료 / 산책 | 응봉산(총 78분, 여유 12분), 한강 벤치(총 55분, 여유 35분) — 정렬 변경 전에는 20분짜리가 먼저 나왔다 |
| 30분 / 무료 / 문화 | Empty 안내, 이전 카드 0개 남음 |
| 출발지와 도착지가 같음 | Error 안내, 결과 영역 숨김 |
| 출발지에 공백만 입력 | Error 안내, 결과 영역 숨김 |
| 모바일 375px | 입력칸·카드 1열, 가로 스크롤 없음, 버튼 높이 50px |
| 프레임워크 미사용 | React·import·require 없음, 외부 링크는 Pico.css CDN 한 줄뿐 |

## 오늘의 한 문장

숫자를 보여줄 때는 그 숫자가 어디서 나왔는지 같이 보여줘야 한다. 근거 없는 `92% Match`보다 `남은 60분 중 12분 여유`가 훨씬 쓸모 있다.

## 다음 작업

- Week 2 Day 1: HTTP, Request, Response, JSON 공부
- Week 2 시작 시 결정할 것: `index.html`과 `frontend/` 중 어느 쪽을 본선으로 이어갈지

---

# 2026-08-29 — 이어서 (Figma · 계획 재작성 · 배포)

## 구현한 것

- Figma 파일 `DETOUR Wireframe v1` 생성. 화면 9개와 각 화면의 발생 조건·담당 주차 주석.
  https://www.figma.com/design/Ca1n7y6VmEO9xDv5EweJTQ
- `PLAN.md` 전면 재작성. Day 1~7 → Session 1~3, 배포를 Week 5에서 Week 2로 이동.
- GitHub Pages 배포. https://nocked115.github.io/DeTour/
- 다크 모드에서 글씨가 안 보이는 문제 수정.

## 고민과 선택

각 고민이 어디에 반영됐는지 함께 적는다.

### ① Figma 와이어프레임을 지금 만들 필요가 있나

- **처음 판단**: 가치가 낮다. 와이어프레임은 "아직 없는 화면을 싸게 그려보는 것"인데
  DETOUR는 이미 동작하는 화면이 있다. 남은 4주 중 화면을 크게 건드리는 건 5주차뿐이다.
- **사용자 선택**: 그래도 만든다. 전체 그림을 눈으로 봐야 한다.
- **만들고 나서 바뀐 생각**: 결과물이 "화면 그림"이 아니라 **할 일 목록**이 됐다.
  9개 중 잘 풀린 경우는 03 하나뿐이고 나머지 8개는 전부 "뭔가 어긋났을 때"다.
  그림으로 늘어놓으니 그게 보였다. 만들자는 판단이 맞았다.
- **연결**: `PLAN.md` Week 3 Session 3(화면 06·07), Week 5 Session 1(화면 08·09),
  `README.md` 디자인 파일 링크.

### ② Week 1이 30분에 끝났으니 5주 계획을 다시 짜야 하나

- **판단**: 다시 짜는 게 맞다. 단, "5주가 5시간이 된다"는 결론은 틀리다.
  Week 1은 5주 중 **유일하게 외부 의존성이 0인 주**였다.
  Week 2~5에는 설치, API 키 발급, CORS 디버깅, 배포처럼 예측 불가한 시간이 있다.
- **더 중요한 것**: 30분에 끝난 건 "구현"이지 "학습"이 아니다.
  AI가 코드를 쓰는 속도는 그 코드를 이해하는 속도가 아니다.
- **연결**: `PLAN.md` "작업 리듬" — 매 세션 마지막 45분은 직접 채우고 기록한다.

### ③ 배포를 언제 하나

- **문제 발견**: 원래 계획은 배포가 Week 5 Day 4였다. **가장 잘 막히는 단계가
  만회할 시간이 없는 자리에 있었다.** 4주 동안 아무도 볼 수 없는 걸 만들다가
  마지막 주에 처음 배포를 시도하는 구조.
- **결정**: Week 2 Session 1로 당긴다. `index.html`은 정적 파일이라 5분이면 된다.
- **추가 근거**: 브라우저 위치 기능(geolocation)은 `file://`에서 아예 작동하지 않는다.
  배포 전에는 테스트 자체가 불가능하다. `fetch()`도 `file://`에서는 CORS로 막힌다.
- **연결**: `PLAN.md` Week 2 Session 1. 그리고 오늘 바로 실행해서 완료.

### ④ 장소 데이터 20개를 직접 조사해야 하나

- **처음 주장**: 성수동 카페 20곳의 가격·체류시간을 사람이 조사해야 한다. 2~3시간.
- **사용자 반박**: 테이크아웃이면 5분, 이런 식으로 대략 정하면 되지 않나.
- **다시 따져보니 반박이 맞다.** 필드별로 나누면 분명하다.
  - 체류 시간·비용 → 조사해도 정확해지지 않는다. 카페가 "40분 앉으세요"라고 정해주지 않는다.
    규칙으로 정하면 값이 일관되고 근거를 설명할 수 있다.
  - 이동 시간 → 손으로 적으면 **오히려 틀린다.** 출발지마다 다르므로 API가 계산해야 한다.
  - 이름·좌표 → 이것만 진짜여야 한다. 그리고 카카오 카테고리 검색으로 자동 수집된다.
- **정정**: 사람 손이 필요한 시간 2~3시간 → 30~60분 (후보 선별, 유형 분류, 규칙표 조정).
- **부수 효과**: 데이터 구조가 바뀐다. 장소마다 시간을 적는 대신 `placeType`을 적고
  표에서 찾아온다. 이게 Week 3 주제인 Feature Engineering의 실제 사례다.
- **연결**: `PLAN.md` "장소 데이터 규칙" 표, Week 3 Session 1.

### ⑤ 서비스 범위가 전국인가 서울인가

- **확인한 것**: 데이터 소스마다 커버 범위가 다르다.
  카카오맵(전국), 기상청(전국), 서울시 실시간 도시데이터(서울 120곳).
- **결론**: 핵심 MVP는 전국이 가능하다. 혼잡도만 서울 한정인데 그건 확장 기능이다.
- **결정**: 목표는 전국, 시작은 성수동. 처음부터 전국으로 하면 추천이 이상할 때
  데이터 문제인지 로직 문제인지 구분할 수 없다.
- **연결**: `PLAN.md` "서비스 범위" 섹션, Week 4 Session 3(좌표 반경 검색으로 교체).

### ⑥ 수업 제출본 기준을 계속 따를 것인가

- **사용자 결정**: `docs/COURSE_WEEK1_BRIEF.md`는 이제 기준으로 쓰지 않는다.
- **영향**: "React/Next 금지"는 수업 규칙이었다. 그게 없어지면 `frontend/`의
  Next.js를 본선으로 삼을지 다시 열린다.
- **보류**: 배포를 막 끝낸 시점이라 지금 파일을 갈아타면 오늘 한 게 날아간다.
  Week 2 시작 시점에 결정한다.

## 막힌 점과 해결 과정

### 다크 모드에서 글씨가 보이지 않음

- **증상**: 노트북에서 `남는 시간을 나만의 우회로로.`, 소개 문구, 결과 제목이 안 보임.
- **원인**: 노트북이 다크 모드라 Pico.css가 `prefers-color-scheme`에 따라 어두운 테마로
  바뀌었다. Pico는 밝은 글씨 색을 골랐는데, 나는 **배경만** 크림색으로 덮어썼다.
  결과적으로 크림색 배경 위에 흰 글씨.
- **재현**: 브라우저를 다크 모드로 놓고 확인. `h1` 색이 `rgb(240,241,243)`,
  `body` 배경이 `rgb(246,242,233)`. 입력 카드까지 검은색이었다.
- **왜 놓쳤나**: 내 테스트 환경이 밝은 테마였다. 색을 지정한 요소(.eyebrow, .helper,
  .breakdown)는 멀쩡했고, **색을 지정하지 않고 Pico에 맡긴 요소만** 깨졌다.
  사용자가 말한 세 곳이 정확히 그 세 곳이었다.
- **해결**: `<html data-theme="light">`로 테마를 고정하고, `h1/h2/h3`와 `.intro`의
  색을 직접 지정.
- **배운 것**: CSS 프레임워크를 부분적으로만 덮어쓰면 위험하다. 배경을 바꿨으면
  글씨도 같이 책임져야 한다. 그리고 **내 화면에서 잘 보인다는 건 증거가 아니다.**

## 오늘의 한 문장

계획에서 가장 중요한 건 무엇을 하느냐가 아니라 **순서**다. 배포가 맨 뒤에 있으면
가장 잘 막히는 단계에 만회할 시간이 없고, 데이터가 로직보다 먼저 진짜가 되면
추천이 이상할 때 원인을 찾을 수 없다.

## 다음 작업

- Week 2 Session 1 나머지: HTTP·HTTPS 공부, README에 배포 URL 추가
- Week 2 시작 시 결정: `index.html`과 `frontend/` 중 무엇을 본선으로 할지
